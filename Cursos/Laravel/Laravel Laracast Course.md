[[Problemas encontrados y solución]]
[[Instalaciones]]

---
Para crear proyectos con el CLI de Laravel se usa el siguiente comando:
```cmd
laravel new <nombre-app>
```
Para observarlo en local se usa el siguiente comando:
```cmd
php artisan serve
```
